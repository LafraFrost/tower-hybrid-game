# Copy this file to `.env` and fill in values for local development
# Example DATABASE_URL format for Postgres:
# postgres://username:password@localhost:5432/tower_hybrid_dev
DATABASE_URL=

# Session secret used for express-session
SESSION_SECRET=change_me_locally

# Optional: PORT to run server on (defaults to 5000)
PORT=5000

## Packages
framer-motion | Smooth layout transitions and complex animations for the map/cards
lucide-react | Icon set for UI elements (already installed but critical)
clsx | Dynamic class composition
tailwind-merge | Merging tailwind classes intelligently
date-fns | Formatting dates for logs

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ['"Orbitron"', "sans-serif"],
  body: ['"Rajdhani"', "sans-serif"],
  mono: ['"Fira Code"', "monospace"],
}
Colors:
Define 'neon-cyan', 'neon-magenta', 'neon-yellow', 'neon-purple' in tailwind config or CSS variables.
Background: Dark hex #050505.
